import { ViewSpecificPartnerPipe } from './view-specific-partner.pipe';

describe('ViewSpecificPartnerPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewSpecificPartnerPipe();
    expect(pipe).toBeTruthy();
  });
});
